---
-
  name: "Converge"
  hosts: "all"
  tasks:
    -
      name: "Include cornfeedhobo.yum_cron"
      include_role:
        name: "cornfeedhobo.yum_cron"
      vars:
        yum_cron_install: true
